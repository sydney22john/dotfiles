# Hot reloading
bind r source-file ~/.config/tmux/tmux.conf 
display "Reloaded :)" 

# binding prefix to be C-space
unbind-key C-Space
set -g prefix C-Space
bind-key C-Space send-prefix 

set -g mouse on

# window renumbering
set -g base-index 1 
setw -g pane-base-index 1 
set -g renumber-windows on 

# rebinding the split commands
bind | split-window -hc "#{pane_current_path}" 
bind - split-window -vc "#{pane_current_path}" 

bind c new-window -c "#{pane_current_path}" 

# vim-like pane switching
bind -r ^ last-window
bind -r k select-pane -U
bind -r j select-pane -D
bind -r h select-pane -L
bind -r l select-pane -R

bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

##############
### DESIGN ###
##############
# Solarized Dark
# default statusbar colors
# set-option -g status-style fg='#ebbcba',bg='#191724'

# default window title colors
# set-window-option -g window-status-style fg='#ebbcba',bg='#191724'
#set-window-option -g window-status-style dim

# active window title colors
# set-window-option -g window-status-current-style fg='#eb6f92',bg='#191724'
#set-window-option -g window-status-current-style bright

# pane border
# set-option -g pane-border-style fg='#c4a7e7'
# set-option -g pane-active-border-style fg='#935fd3'

# message text
# set-option -g message-style fg='#31748f',bg="#191724"

# pane number display
# set-option -g display-panes-active-colour brightred #orange
# set-option -g display-panes-colour blue #blue

# clock
# set-window-option -g clock-mode-colour green #green

# bell
# set-window-option -g window-status-bell-style fg=black,bg=red #base02, red
